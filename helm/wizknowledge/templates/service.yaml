apiVersion: v1
kind: Service
metadata:
  name: {{ include "wizknowledge.fullname" . }}
  labels:
    {{- include "wizknowledge.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wizknowledge.selectorLabels" . | nindent 4 }}